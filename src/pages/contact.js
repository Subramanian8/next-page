import { Stack, Typography } from "@mui/material";

function Contact() {
    return <Stack spacing={5} mt={10} mb={20}>
        <Typography>
            Contact
        </Typography>

    </Stack>
}

export default Contact;